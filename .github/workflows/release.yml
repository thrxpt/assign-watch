name: Release

permissions:
  contents: write

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build extension
        run: |
          pnpm zip
          pnpm zip:firefox

      - run: npx changelogithub --assets ".output/*-chrome.zip,.output/*-firefox.zip"
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
